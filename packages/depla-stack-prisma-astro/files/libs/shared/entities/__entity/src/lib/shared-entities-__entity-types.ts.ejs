import { <%= entity.ref %>Collection } from '<%= workspace.scope %>/shared/generated/entities/<%= entity.ref %>';
import type { I<%= entity.model %> } from '<%= workspace.scope %>/shared/generated/entities/<%= entity.ref %>';

export { <%= entity.ref %>Collection, I<%= entity.model %> }
