import { <%= entity.model %> as <%= entity.model %>GeneratedModel } from '<%=
workspace.scope %>/shared/generated/entities/<%= entity.ref %>';
import { config } from './shared-entities-<%= entity.ref %>-config';

export function <%= entity.model %>(app) {
  const <%= entity.ref %>Model = <%= entity.model %>GeneratedModel(app, config)

  return Object.assign(<%= entity.ref %>Model, {
    /**
     * Custom method (feel free to modify / remove)
     */
    getSomething() {
      return 'you just called <%= entity.ref %>.getSomething() method'
    },
  });
}
