# generate-code

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build generate-code` to build the library.

## Running unit tests

Run `nx test generate-code` to execute the unit tests via [Jest](https://jestjs.io).
