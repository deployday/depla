import { app } from '<%= workspace.scope %>/generated/shared/app';
import { collectionsProvider } from '<%= workspace.scope %>/shared/util/astro-collections-facade';

collectionsProvider(app);

export { app };
