import { defineCollection } from 'astro:content';
import { postCollection } from '<%= workspace.scope %>/shared/entities/post';

export const collections = {
  post: defineCollection(postCollection),
};
