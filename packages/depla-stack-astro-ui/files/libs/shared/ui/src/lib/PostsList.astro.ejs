---
import { PostsListItem as Item } from '<%= workspace.scope %>/shared/ui';
import type { IPost } from '<%= workspace.scope %>/shared/entities/post';

export interface Props {
  posts: Array<IPost>;
}

const { posts } = Astro.props;
---

<ul>
  {
    posts.map((post) => (
      <li class="mb-12 md:mb-20">
        <Item post={post} />
      </li>
    ))
  }
</ul>
