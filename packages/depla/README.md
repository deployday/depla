# depla

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build depla` to build the library.

## Running unit tests

Run `nx test depla` to execute the unit tests via [Jest](https://jestjs.io).
